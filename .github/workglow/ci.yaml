name: Basic CI

on:
  push:
    branches: [feature-branch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run build
        run: echo "CI is running..."